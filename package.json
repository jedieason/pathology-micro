{
  "name": "pathology-micro",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "A static pathology slide practice app for GitHub Pages",
  "scripts": {
    "start": "python3 -m http.server 8080 --bind 127.0.0.1",
    "test": "node --test tests/*.test.js"
  }
}
